"# TrackTheObjInNext3Img" 

if used proxy,pls use the following when upload with git
git config --global http.proxy http://user:password@proxy:8080
git config --global https.proxy https://user:password@proxy:8080
